class Solution {
public:
    int passThePillow(int n, int time) {
        int x = time/(n-1);
        time = time % (n-1);
        if(x&1){
            return n-time;
        } 
        else
            return 1+time;
    }
};
